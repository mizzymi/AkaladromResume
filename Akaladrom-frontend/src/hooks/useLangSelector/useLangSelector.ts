/**
 * **DESCRIPTION:**
 * 
 * With this interface we can represent the return of the
 * custom hook of the useLangSelector, that this custom
 * hook will return only one property that an object of this
 * type.
 */
interface UseLangSelectorReturn {

}

/**
 * **DESCRIPTION:**
 * 
 * You need to write a description of what the useLangSelector do on here.
 * 
 * **RETURNS:**
 * 
 * @returns returns an object with the properties of: 
 * * need to write it
 * 
 * **EXAMPLE OF USE:**
 * 
 * @example
 * const { } = useLangSelector({}); 
 */
export const useLangSelector = ({ }: UseLangSelectorProps): UseLangSelectorReturn => {

    return {

    }

}
